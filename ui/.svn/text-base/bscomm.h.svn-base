#ifndef BSCOMM_H
#define BSCOMM_H
#include <QString>

class bscomm
{
public:
    bscomm();


    double static GetCountAbsorbancy(double dBaseSignalVal,double dSampleSignalVal);
    int static GetHgSignSpace(int iSpaceType);//获取基线值、样品值所读取汞信号模块的时间间隔类型
    bool static isNumber(QString qsValue);
    double static Round(double dbVal, int nPrec /* = 2 */);
   // bool static IsNumeric(QString _strValue);

};

#endif // BSCOMM_H
